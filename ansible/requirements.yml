---
# Ansible collections required by muffin_stack.yml + roles (pinned to versions verified locally).
collections:
  - name: ansible.posix
    version: "1.6.2"
  - name: community.general
    version: "10.7.3"
  - name: community.docker
    version: "4.7.0"
